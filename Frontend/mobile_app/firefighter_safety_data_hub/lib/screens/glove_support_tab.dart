import 'package:flutter/material.dart';

class GloveSupportTab extends StatelessWidget {
  const GloveSupportTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Glove Support'),
      ),
      body: Container(),
    );
  }
}
