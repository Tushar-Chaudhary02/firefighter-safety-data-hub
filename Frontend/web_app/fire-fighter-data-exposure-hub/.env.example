# Optional. Vite already defaults to the local backend on port 8000.
API_PROXY_TARGET=http://127.0.0.1:8000
VITE_APP_NAME=Firefighter Safety Data Hub

# Set this only when the browser should call an API directly rather than via Vite.
# VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1/web_api
